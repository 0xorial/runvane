/** Default model for e2e/integration (`stub-model` in seed + picker). */
export const STUB_E2E_MODELS = ['stub-model'] as const;

/** Model names shown in demo recordings (multi-model picker, activity labels). */
export const STUB_DEMO_MODELS = ['gpt-4o', 'gpt-4o-mini', 'claude-sonnet-4.6'] as const;
