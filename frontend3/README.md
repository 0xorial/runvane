# Runvane Frontend3 (Svelte)

Svelte 5 rewrite targeting feature parity with `frontend/`. Shares the same Playwright e2e suite.

See [docs/frontend3-parity-plan.md](../docs/frontend3-parity-plan.md) for the rollout plan.

```bash
npm install
npm run dev       # dev-ports slot frontend3 → :52205
npm run test:e2e  # shared tests from ../frontend/e2e
```
